# Test-only MQTT broker for the harness. Anonymous, plaintext — this is NOT the
# production broker. The hardened mosquitto (TLS, per-device credentials, ACLs)
# lands in M3.1; the synthetic sensor fleet points here for now.
listener 1883 0.0.0.0
allow_anonymous true
persistence false
log_dest stdout
