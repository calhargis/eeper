// Phase 0: the PWA is served as a static single-page app (adapter-static
// fallback), with no server-side rendering at runtime. Later phases add the
// real views (Live, Tonight, Trends, Devices, Settings).
export const ssr = false;
export const prerender = false;
